<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Ana Tema: MaterialComponents kullanarak daha stabil hale getirildi -->
    <style name="Theme.Nightbrate" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Birincil Renkler -->
        <item name="colorPrimary">@color/primary_green</item>
        <item name="colorPrimaryVariant">@color/bg_dark</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- İkincil Renkler -->
        <item name="colorSecondary">@color/primary_green</item>
        <item name="colorSecondaryVariant">@color/bg_dark</item>
        <item name="colorOnSecondary">@color/black</item>

        <!-- Ekran Arka Planı: Siyah ekran sorununu çözmek için eklendi -->
        <item name="android:windowBackground">@color/bg_dark</item>
        <item name="android:statusBarColor">@color/bg_dark</item>
    </style>

    <style name="Theme.Nightbrate.NoActionBar" parent="Theme.Nightbrate">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.Nightbrate.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />
    <style name="Theme.Nightbrate.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>