<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Sabit açık tema (DayNight yok); setDefaultNightMode ile çakışan yeniden oluşturma yok. -->
    <style name="Theme.Nightbrate" parent="Theme.Material3.Light.NoActionBar">
        <item name="colorPrimary">@color/primary_green</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSecondary">@color/primary_green</item>
        <item name="colorOnSecondary">@color/white</item>
        <item name="colorSurface">@color/card_surface</item>
        <item name="colorOnSurface">@color/text_primary</item>
        <item name="android:colorBackground">@color/client_scaffold</item>
        <item name="colorOnBackground">@color/text_primary</item>

        <item name="android:windowBackground">@color/client_scaffold</item>
        <item name="android:statusBarColor">@color/client_scaffold</item>
        <item name="android:navigationBarColor">@color/nav_bar_bg</item>
        <item name="android:windowLightStatusBar" tools:targetApi="m">true</item>
        <item name="android:windowLightNavigationBar" tools:targetApi="o_mr1">true</item>
        <item name="android:windowDrawsSystemBarBackgrounds" tools:targetApi="lollipop">true</item>
        <!-- API 35+ (ör. Baklava emülatörü): varsayılan edge-to-edge bazen ilk frame’de siyah/boş pencere; giriş ekranı kayboluyor -->
        <item name="android:windowOptOutEdgeToEdgeEnforcement" tools:targetApi="35">true</item>
    </style>

    <style name="Theme.Nightbrate.NoActionBar" parent="Theme.Nightbrate">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <!-- Launcher: splash → giriş teması (API 31+ / 36 önizlemede siyah ilk kare) -->
    <style name="Theme.Nightbrate.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/client_scaffold</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>
        <item name="postSplashScreenTheme">@style/Theme.Nightbrate.NoActionBar</item>
        <item name="android:windowOptOutEdgeToEdgeEnforcement" tools:targetApi="35">true</item>
    </style>

    <style name="Theme.Nightbrate.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />
    <style name="Theme.Nightbrate.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>
